# Copy to .env and fill in values. Loaded automatically at build time.
#
# Required when using library-pdf components
LITURGICS_LIBRARY_BUCKET=

# Optional; boto3 uses its default credential chain otherwise
AWS_REGION=
